//
//  SampleAPI.swift
//  dnd-5th-1-iOS
//
//  Created by taeuk on 2021/07/14.
//

import Foundation

struct SampleAPI {
    /*
    static func fetchURLSession(completion: @escaping resultModel<[Course]>) {
        
        let urlString = "https://api.letsbuildthatapp.com/jsondecodable/courses"
        
        let request = URLRequest(url: URL(string: urlString)!, method: HTTPMethod<[Course]>.get)
        
        URLSession.shared.request(request) { (response: Result<[Course], APIError>) in
            switch response {
            case .success(let data):
                
                completion(.success(data))

            case .failure(let err):
                completion(.failure(err))
            }
        }
    }
     */
}
